$(document).ready(function(){
    $('.scrollspy').scrollSpy();
    $('.tooltipped').tooltip();
    $('.fixed-action-btn').floatingActionButton({
        direction: 'left',
        hoverEnabled: false
    });
    $('.modal').modal();
    $('.materialboxed').materialbox();
});